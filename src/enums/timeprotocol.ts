export enum TimeProtocol {
	Smpte = 0,
	FrameCount = 1,
	Default = 2,
}

// TODO: remove those
export const FBX_TIME_PROTOCOL_SMPTE = 0;
export const FBX_TIME_PROTOCOL_FRAME_COUNT = 1;
export const FBX_TIME_PROTOCOL_DEFAULT = 2;
